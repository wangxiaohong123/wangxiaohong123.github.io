##### 服务端todo

1. 马甲包逻辑
2. 后台手动通过动态时，如果文本违规动态状态会变成待人工审核
3. nlp和媒体内容识别
4. 审核接入模型
5. 订单逻辑
6. 用户id生成逻辑，cms返回string
7. oss群头像大量重复，群头像去掉oss
8. 反馈时出现用户id=1的情况
9. 出现会员权益未刷新
10. 使用Vue-Pure-Admin重写管理后台
11. cdn鉴权，oss私有读，cnd回源私有bucket（ios版本上线、大部分用户更新之后，现在oss的域名是cdn，修改oss权限会导致资源无法上传）
12. sae迁移
13. 接收回应过滤掉已经失效的会话
14. ios支付，对账
15. cmd收入统计不准
16. 超级曝光出现->预扣减星币过多？或者预扣减之后没有扣减或者还原
17. sentinel换成阿里云
18. 注销用户删除：成长值记录、访客记录
19. mq的topic混乱

### 项目

```shell
# 脚本自启
# 修改sh脚本中的~，systemd不认识~符号

# 创建service文件
vim /etc/systemd/system/stars-content.service
# 插入以下内容
[Unit]
Description=Stars Content Production Service (Root)
After=network.target
[Service]
Type=forking
User=root
Group=root
# 显式使用 sh 执行，避免脚本首行解释器识别错误
ExecStart=/usr/bin/sh /root/server/content-prod.sh start
ExecStop=/usr/bin/sh /root/server/content-prod.sh stop
# 告诉 systemd，只要这个脚本成功返回 0 或 143，就认为启动成功
SuccessExitStatus=143
Restart=on-failure
RestartSec=10s
[Install]
WantedBy=multi-user.target


# 重新加载配置
sudo systemctl daemon-reload
# 设置开机自启
systemctl enable stars-content
# 手动启动服务
systemctl start stars-content
#查看服务详情
systemctl status stars-content
```

##### 部署 （跟用户有关的业务要考虑客服！！！）

###### 部署服务

###### 执行sql

```sql

```

###### mq操作



###### 其他



##### 
