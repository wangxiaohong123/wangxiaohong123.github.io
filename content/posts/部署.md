### ios todo

1.   获取token增加deviceId

##### 服务端todo

1.   cms审核重写
2.   冻结缓存逻辑，冻结删除最后一条token
3.   内容审核->多图审核逻辑
4.   马甲包逻辑
5.   nlp和媒体内容识别
6.   审核接入模型
7.   订单逻辑
8.   用户id生成逻辑，cms返回string
9.   oss群头像大量重复，群头像去掉oss
10.   反馈时出现用户id=1的情况
11.   出现会员权益未刷新
12.   cdn鉴权，oss私有读，cnd回源私有bucket（ios版本上线、大部分用户更新之后，现在oss的域名是cdn，修改oss权限会导致资源无法上传）
13.   sae迁移
14.   长时间不联系不能发消息
15.   接收回应过滤掉已经失效的会话
16.   ios支付，对账
17.   cmd收入统计不准
18.   超级曝光出现->预扣减星币过多？或者预扣减之后没有扣减或者还原
19.   sentinel换成阿里云
20.   mq消费者没有日志id



```vue
<!-- 新分页修改 方法 -->
methods: {
  // 新增：加载数据的统一方法
  loadTableData({ cursor, pageSize }) {
    this.common.toTop();
    // 更新搜索参数
    this.searchData.curPageMinId = cursor;
    this.searchData.size = pageSize;
    // 调用原有的获取数据方法
    this.getTableData();
  },

import CursorPaginationTable from '@/components/CursorPaginationTable.vue';
components: {
    CursorPaginationTable
  },
  
  // 修改：搜索时重置组件
  handleSearchData() {
    this.$refs.paginationTable.reset();  // 重置分页组件
    this.searchData.curPageMinId = null;
	……
    this.getTableData();
  },
}
```

##### bert训练语料

```sql
SELECT content FROM violation_remark_record 
WHERE type=0 AND risk_label1 IN ('politics','violence','ban','minor') AND other_id is null 
ORDER BY id DESC LIMIT 1000

SELECT i.content FROM explore_msg_item i 
LEFT JOIN explore_msg m ON i.explore_msg_id=m.id 
WHERE i.msg_type=0 AND m.`status` < 3 AND m.porn=0 AND m.nlp_porn=0 
ORDER BY i.id DESC LIMIT 1000
```



##### 部署

**跟用户有关的业务要考虑客服！！！**

```sql
CREATE TABLE `msg_repeat` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '用户 id',
  `text` varchar(255) NOT NULL COMMENT '文本内容',
  `hash` varchar(255) DEFAULT NULL,
  `num` int(11) NOT NULL COMMENT '此用户发送此消息次数',
  `receivers_num` int(11) NOT NULL COMMENT '接收此人此消息的人数量',
  `receivers_ids` text NOT NULL COMMENT '接收者们的 id，格式为 id1,id2',
  `update_time` bigint(20) NOT NULL DEFAULT '0' COMMENT '最后更新时间',
  PRIMARY KEY (`id`) USING BTREE,
  KEY `ix_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='重复记录';

ALTER TABLE `stars_identification`.`msg_meta` 
DROP COLUMN `num`,
DROP COLUMN `senders_num`,
DROP COLUMN `p`,
DROP COLUMN `an_porn_score`,
DROP COLUMN `an_non_porn_score`,
CHANGE COLUMN `sm_risk_types` `risk_type` int(11) NULL DEFAULT NULL COMMENT '风险类型' AFTER `update_time`;

ALTER TABLE `stars_identification`.`violation_image_record` 
CHANGE COLUMN `risk_types` `risk_type` int(11) NULL DEFAULT 0 COMMENT '风险类型' AFTER `handling_id`;

ALTER TABLE `stars_identification`.`violation_remark_record` 
ADD COLUMN `risk_type` int(10) NULL COMMENT '违规类型' AFTER `type`;
```

先部site,设置提示词。

#### 审核

```shell
curl -X POST http://39.98.179.24:5000/moderate -H "Content-Type: application/json" -d '{"content": "你这个傻逼"}'

curl -X POST http://39.98.179.24:5000/moderate -H "Content-Type: application/json" -d '{"content": [{"userid":1, "content":"你好"}, {"userid":2, "content":"我要杀了你"}]}'
```

1.   冻结
2.   内容识别的框架、训bert
3.   审核去掉一审、二审



探索发送：deepseek -> 涉黄调用nlp；

文本内容审核2个接口：

1.   广播：探索发送，动态，用户信息
2.   私聊：聊天（异步），探索回应（实时）









